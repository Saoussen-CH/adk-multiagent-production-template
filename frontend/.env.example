VITE_API_URL=http://localhost:8000
# Placeholder for the store identifier a real embed-script mechanism would
# supply per-widget (out of scope for this plan) — see ChatRequest.tenant_id.
VITE_TENANT_ID=demo-store
